@model F_Result.Models.PlanningPeriod

@{
    ViewBag.Title = "Удаление";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container">
    <div class="col-lg-12">
        <h1 class="page-header">
            @ViewBag.Title
            <small>Удаление планового периода</small>
        </h1>
        <ol class="breadcrumb">
            <li>@Html.ActionLink("Главная", "Index", "Home", new { Area = "" }, null)</li>
            <li>Справочники</li>
            <li>@Html.ActionLink("Плановые периоды", "PPShow", "PlanningPeriods")</li>
            <li class="active">Удаление</li>
        </ol>
    </div>

    <h3 class="text-danger">Вы действительно хотите удалить выбранную запись?</h3>
    <hr />
    <div>
        <dl class="dl-horizontal">
            <dt>
                Код
            </dt>

            <dd>
                @Html.DisplayFor(model => model.PlanningPeriodId)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.PeriodName)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.PeriodName)
            </dd>

        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color">
                <a class="btn btn-primary" href='@Url.Action("PPShow")'><span class="glyphicon glyphicon-circle-arrow-left"></span>&nbsp;Назад</a>
                <button type="submit" title="Удалить запись" class="btn btn-primary"><i class="glyphicon glyphicon-trash"></i>&nbsp;Удалить</button>
            </div>
        }
    </div>
</div>